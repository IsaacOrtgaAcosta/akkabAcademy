version: "3.9"

services: 
  backend:
    build: ./backend
    container_name: mi-backend
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=development

  frontend:
    build: ./frontend
    container_name: mi-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend